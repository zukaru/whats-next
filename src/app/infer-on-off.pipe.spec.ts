import { InferOnOffPipe } from './infer-on-off.pipe';

describe('InferOnOffPipe', () => {
  it('create an instance', () => {
    const pipe = new InferOnOffPipe();
    expect(pipe).toBeTruthy();
  });
});
